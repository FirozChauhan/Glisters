.env
node_modules/
.freebuff/
worker/diagnose-jwt.mjs

# wrangler local cache — can contain account/oauth credentials
worker/.wrangler/
worker/.dev.vars
.dev.vars

# OS / editor cruft
.DS_Store
*.log
worker/.wrangler/
worker/.dev.vars

# js/config.js + js/auth.js are COMMITTED (public-scope values only) so
# GitHub clones work out of the box; js/auth.js is a plain copy of js-src/auth.js

# release zips
/dist/
